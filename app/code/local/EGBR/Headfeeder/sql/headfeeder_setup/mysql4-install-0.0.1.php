<?php
/*
	// This is where we should add the extra attribute to the products table
	$installer = $this;
	
	// Create the new attribute 
	$installer->installEntities();
	
        Mage::log("Criando novo atributo 'feed_status' em ".date('h:i:s d/m/Y'));
	// and populate it with the default value
	try{
            $installer->createNewAttributeSet('feed_status');
        }catch(Exception $e){
           Mage::log($e->getMessage());
        }
*/       
